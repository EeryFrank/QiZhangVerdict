# Minecraft 1.12.2 真实客户端验收

2026-09-25，使用官方 Minecraft 1.12.2、Forge 14.23.5.2864 和 Temurin Java 8u504，以生产成品模组 JAR 分别连接匹配的 Forge 专服与 Paper 1620 插件服。两轮最终验收通过；这是本机合成账号、离线认证夹具的联机测试，不证明正版认证链路已通过。

| 连接组合 | 报告获准后持续观察 | 最后一次新查询 | 客户端 / 专服退出码 |
| --- | --- | --- | --- |
| Forge 客户端 → Forge 14.23.5.2864 | 65.354 秒 | 生存模式测试成功 | 0 / 0 |
| Forge 客户端 → Paper 1.12.2 build 1620 | 65.271 秒 | 生存模式测试成功 | 0 / 0 |

客户端和 Forge 专服均使用 `qizhangverdict-forge-1.12.2-0.2.0-dev.jar`，SHA-256 为 `7ee00ced99919281e2d6851e123c6bcb2d8ddd3e46338870b2034d5a5dcf0966`。Paper 插件服使用已公开的 GPL `qizhangverdict-bukkit-0.1.1.jar`，SHA-256 为 `e1194b8b694763afbc431e22799f40d357dd80493d194f680edfb219afe94845`。Paper 1620 的文件 SHA-256 为 `3a2041807f492dcdc34ebb324a287414946e3e05ec3df6fd03f5b5f7d9afc210`，与官方固定下载记录一致。这里的 1.12.2 本地成品不能替换成不同哈希的 CI 重编产物后沿用运行结论。

两端都保持完整默认策略：配套报告必需、设备标识必需、VM `DENY`、报告超时 20 秒及默认 IP/设备配额。运行前后的配置 SHA-256 均为 `47310c31b47287ab805bfdbf8ddddf80e8169385bd9c862239a0a0075644da4e`。独立复核确认，持久状态中存在本轮精确测试 UUID 的设备关联；公开证据只保留此布尔结论，不包含设备摘要或系统安装标识。

Forge 的报告等待阶段使用旁观隔离，之后生存模式选择器成功。Bukkit 的等待隔离不依靠切换游戏模式，因此 **Bukkit 的生存模式本身不是报告获准证明**：脚本先确认该精确 UUID 的设备关联已落盘，再开始计时；超过 65 秒后重新发送 `testfor @a[name=VerdictClient,m=0]`，只接受新追加日志中的成功回复。两轮持续时间均来自 `time.monotonic()`，没有用旧日志命中或时钟差值代替最后的在线检查。

摘要使用 `survival_mode_confirmed`，表示已确认生存模式。原始结果保留历史字段 `restored_survival`，在 Bukkit 结果中该字段只对应生存模式查询成功，不表示曾经切换过游戏模式。公开摘要明确说明此区别，没有改写原始结果。

两轮都完成图形和声音初始化，无本模组资源包元数据警告；本轮 PNG 均为 640×360，已核验 CRC、解压、尺寸和 SHA-256，并实际查看到生存 HUD。最终执行脚本分别为：

- Forge：`7a5b0d1938090c1ce3d0f709a20ac0d4ff71bfee144585db40a43ffa803f55b8`。
- Bukkit：`f1b8c30104b60e01057838be8c6640bd432ed406663a9108d0c7834c1fa28043`。

两次早期失败均保留，没有改成通过：

- Forge 首轮已完成报告与生存观察，但 QA 错用 `WM_CLOSE` 关闭 LWJGL2 窗口，Java 退出请求没有按预期生效。关闭超时后，该客户端被终止，退出码 1；专服退出码 0。随后只将 QA 改为库实际支持的 `WM_SYSCOMMAND / SC_CLOSE`。原始库字节码的只读核验摘要公开，完整反汇编留在临时目录。首轮原始错误包含完整合成客户端启动命令，因此只公开明确标为派生的失败摘要，并保留原始结果路径和 SHA-256。
- Bukkit 首轮实际报告和设备关联已成功，但旧 Paper 没有预期的加入广播日志，QA 未识别玩家入服，未完成通过条件。操作者正常关闭了本轮客户端，两端退出码均为 0，该轮仍为失败。之后只修正 QA，使其匹配精确的本机 `PlayerList` 登录行。

上述修正没有改变产品、游戏或库文件。后来对 QA 字段名和错误摘要的文字澄清，也没有被冒充为一次新的运行；验收引用的始终是当时执行脚本的真实哈希与快照。

[机器可读验收摘要](../outputs/legacy-client-matrix/1.12.2-0.2.0-dev-gpl/acceptance.json) 索引了原字节日志、通过结果、Bukkit 首轮结果、Forge 首轮派生摘要、截图、实际执行的脚本快照及其校验值。公开文件不含 `accounts.state`、原始设备哈希、系统安装 ID、完整启动计划或完整反汇编。快照用于核验历史逻辑；复现应使用项目中的 [legacy112-client-smoke.py](../platforms/legacy112-client-smoke.py)，选择新的运行目录并重新记录结果，不能直接把证据目录当成源码工程运行。

本轮未覆盖真实虚拟机厂商矩阵、伪造报告对抗、大型整合包、旧存档、长期性能或正版基础设施。设备和虚拟机信息仍来自客户端自报，不是可信硬件证明。
