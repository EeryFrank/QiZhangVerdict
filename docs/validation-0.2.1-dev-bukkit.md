# Bukkit 0.2.1-dev 验收

本次只验证 SHA-256 `9849a60374ea1bb18aae2af0ee479593312437a9665b803a3f847a07c570b2c8` 的 74,976 字节成品。[完整报告与原始附件白名单](../outputs/bukkit-validation-0.2.1-dev.json)记录来源、命令、脚本快照、哈希和实际退出结果。

| 验证范围 | 结果 |
| --- | --- |
| 从实际 Bukkit JAR 加载核心，原生 Java 8 | 57 安全 + 37 目录 + 3 保留管理员配置对照，共 97 项通过 |
| Purpur 1.20.1 build 2062，Java 17 | 26 组真实 TCP 通过；协议进程与服务器退出 0，正常保存停机 |
| Purpur 1.21.1 build 2329，Java 21 | 26 组真实 TCP 通过；协议进程与服务器退出 0，正常保存停机 |

两服顺序运行，均为新世界和新插件数据。首次启动完整 13 项默认配置逐项核对，默认文件散列均为 `47310c31b47287ab805bfdbf8ddddf80e8169385bd9c862239a0a0075644da4e`，日志确认 37 条规则、伴生客户端和设备报告必需、VM DENY。随后为了批量 TCP 检查，脚本明确把历史账号上限从 5 调为 100、每分钟尝试从 20 调为 1000、报告超时从 20 秒调为 4 秒、VM 从 DENY 调为 ALERT；特定用例另用 10 秒命令门禁、CIDR 拒绝、VM DENY 和每 IP 最多 2 人。`companion.required=true`、`device.required=true`、同 IP 同设备最多 1 人和 `sanctions.on-deny=BAN` 保持启用。

52 组覆盖正常/缺失/畸形报告、实际 OP 待验证命令阻断及验证后执行、同 IP 同设备 1 人/异设备 3 人/配置 2 人、退出释放配额、设备和账户封禁及管理解封、模糊黑名单/白名单/删除默认规则、CIDR、已知 VM 标记和单独 VBS 标记。客户端是合成报告的真实 TCP 模型；不等于图形伴生客户端、真实虚拟机、全部作弊检测、性能或第三方防作弊集成验收。

原协议依赖为 `minecraft-protocol 1.66.2`、`minecraft-data 3.117.0`、`protodef 1.19.0`，固定执行脚本 `d133f0c0…`。1.21.1 的原协议日志保留 22 次 `PartialReadError`（`ArmorTrimMaterial` / `SlotComponent` 解码栈），1.20.1 为 0；上述 26 个实际断言各自完成，不能据此宣称全部 Minecraft 数据包解码无误，具体解码原因未在本次独立定位。服务器的旧版插件 API、离线模式、终端及 SIMD 告警同样保留。没有隐藏告警或换脚本重写原结果。

[join/reload 挑战竞态报告](bukkit-challenge-race.md)单独记录旧发布物真实失败复现与新成品修复；不把旧版的 5 个复现检查当通过数，也不把该报告检查混入这里的 97 或 52。这里亦不沿用历史旧 JAR 的 75/42 组运行计数。

公开附件不含 scope、账户状态、真实设备标识、世界、Minecraft 二进制或测试生成的私有状态。报告中的合成账号/UUID与固定测试标签摘要可公开。结束后四个自有服务器/Node PID 均已退出，25685/25686 经独占绑定确认释放。
