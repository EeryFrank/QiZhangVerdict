# Forge 1.8.9 默认策略 TCP 验收

2026-09-25，QiZhangVerdict `0.2.0-dev` GPL 成品在 Forge `1.8.9-11.15.1.2318-1.8.9` 独立专服完成 **14 组真实 TCP 断言**，协议客户端和服务器均正常退出，退出码均为 `0`。这是合成协议客户端验收，不是图形客户端或完整反作弊准确率验收。

[完整结果与原始证据索引](../outputs/legacy-mod-tcp-validation-1.8.9-gpl.json)包含每项断言、原始文件路径、SHA-256 和大小。6 份公开附件共 47,835 字节，按原字节复制；服务器警告和错误没有删改。

| 项目 | 实测值 |
| --- | --- |
| Guard 成品 SHA-256 | `f00943b06e87dbfc04e88336134ab4ae1a571a6e9b53924ab6bc06b311918424` |
| Forge 服务器入口 SHA-256 | `596512ad5f12f95d8a3170321543d4455d23b8fe649c68580c5f828fe74f6668` |
| 测试脚本 | [legacy189_mod_smoke.py](../scripts/legacy189_mod_smoke.py) |
| Java / 最大堆 | Temurin `1.8.0_504-b01` / 1536 MiB |
| 实际协议 / 通道 | `47` / `QZGuard`、`REGISTER` |
| 测试地址 | 仅 `127.0.0.1:25662`，隔离测试使用离线认证 |
| 有效报告后持续在线 | 22.071 秒，随后新的控制台状态确认 strict 会话仍在线 |
| 缺失报告超时 | 20.945 秒后断开 |
| 整轮运行时间 | 79.68 秒，正常保存三个维度并停服 |

新目录只从已停止的基础夹具复制官方入口、原版服务器及 18 个依赖库。没有复制世界、玩家数据、Guard 配置或账号状态。测试前后完整 `guard.properties` 都与固定成品中的默认常量逐字节一致，SHA-256 为 `47310c31b47287ab805bfdbf8ddddf80e8169385bd9c862239a0a0075644da4e`，没有提高账号限额、关闭设备验证或缩短报告期限。

准入检查覆盖：有效报告；同 IP 同设备第二账号拒绝；同 IP 三个不同设备允许、第四个并发账号拒绝；退出释放并发额度；五个成功账号之后第六个不同账号触发滚动账号限制；缺失设备和畸形报告拒绝且不永久封号；缺失报告超时，以及超时后原账号重新登录。最终状态保存 5 个合成账号，没有账号或设备永久禁令。

命令门禁使用实际控制台授权的 OP。报告未提交时，客户端收到明确的等待验证拒绝回应，`QZ189_PRE_GATE` 没有执行；提交有效报告后，同一连接执行的 `QZ189_POST_GATE` 出现在服务器日志。它证明本次 Forge CommandEvent 门禁实际生效，不依赖现代版本的 Mixin。

原始日志保留 8 条 WARN 与 1 条 ERROR。ERROR 是旧 Forge 的 `FML appears to be missing any signature data`；WARN 包括隔离离线认证提示和首次不存在的禁令、OP、白名单文件。服务器仍正常启动、完成断言并退出，不能据此称为无告警启动。

协议库将协议 47 的数据别名标记为 `1.8.8`，实际服务器与 Guard 模组均为 `1.8.9`；该结果不证明可以把此模组安装到 Forge 1.8.8。报告中的设备值和模组列表由合成客户端自报，不能证明真实硬件身份、虚拟机识别准确率、抵抗篡改客户端、透视屏蔽、战斗/移动检测、任意整合包兼容或性能。账号状态、世界、服务器作用域标识以及第三方二进制均未纳入公开附件。
