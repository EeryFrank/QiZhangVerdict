allprojects {
    group = 'cn.qizhang.guard'
    version = '0.2.1-dev'
    repositories { mavenCentral() }
}
subprojects {
    ext.qvLicenseRoot = rootProject.projectDir
    apply from: rootProject.file('gradle/license-resources.gradle')
}
